{{#partial "contenido" }}
<body>
    <main>
        <section>

            <h2 class="title">Paradas</h2>
            <table class="table m-auto">
                <thead>
                    <tr>
                        <th>Nombre de Parada</th>
                        <th>Provincia</th>
                        <th>Municipio</th>
                        <th>Calle</th>
                        <th>Altura</th>
                    </tr>
                </thead>
                <tbody>
                {{#each parada}}
                    <tr>
                        <td>{{nombre}}</td>
                        <td>{{ubicacion.provincia}}</td>
                        <td>{{ubicacion.municipio}}</td>
                        <td>{{ubicacion.calle}}</td>
                        <td>{{ubicacion.altura}}</td>
                    </tr>
                {{/each}}
                </tbody>
            </table>

        </section>
    </main>


    <footer class>
    </footer>

</body>
{{/partial}}
{{> base.hbs }}