{{#partial "contenido" }}
<body>
    <main>
        <section>

            <h2 class="title">Miembros</h2>
            <table class="table m-auto">
                <thead>
                    <tr>
                        <th>Nombre de Miembro</th>
                    </tr>
                </thead>
                <tbody>
                {{#each miembro}}
                    <tr>
                        <td>{{nombre}}</td>
                        <td><button style="background-color: #77a785;" class="button is-primary">Funar</button></td>
                    </tr>
                {{/each}}
                </tbody>
            </table>

        </section>
    </main>


    <footer class>
    </footer>

</body>
{{/partial}}
{{> base.hbs }}