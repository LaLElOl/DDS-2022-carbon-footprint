{{#partial "contenido" }}
<body>
    <main>
        <section>
            {{#if agenteProvincial}}
            <h2 class="contacto title">
            Editar Agente Provincial
            </h2>
            {{else}}
            <h2 class="contacto title">
            Alta de Agente Provincial
            </h2>
            {{/if}}

            <form class="formulario" method="post">
                <fieldset>
                    <div class="campo">
                        <label>Usuario</label>
                        <input name="usuario" type="text" placeholder="Usuario" value="{{agente_provincial.usuario.nickname}}">
                    </div>

                    <div class="campo">
                        <label>Email</label>
                        <input name="email" type="email" placeholder="Email" value="{{agente_provincial.usuario.email}}">
                    </div>

                    <div class="campo">
                        <label>Contraseña</label>
                        <input name="contrasenia" type="password" placeholder="Contraseña" value="{{agente_provincial.usuario.contrasenia}}">
                    </div>

                    <div class="campo">
                        <label>Nombre</label>
                        <input name="nombre" type="text" placeholder="Nombre" value="{{agente_provincial.nombre}}">
                    </div>

                    <div class="campo">
                        <label>Provincia</label>
                        <input name="provincia" type="text" placeholder="Provincia" value="{{agente_provincial.provincia}}">
                    </div>

                    <div class="enviar">
                        <button class="boton" type="submit">Enviar</button>
                    </div>
                </fieldset>
            </form>
        </section>
    </main>
</body>
{{/partial}}
{{> base.hbs }}